"""Minimal runnable research demo: one-feature linear regression.

Pure standard library except for ``pyyaml`` (config files). Intended to
demonstrate the trellis-for-research retained-run flow:
data manifest -> config -> train -> retained run manifest -> curated report.
"""

__version__ = "0.1.0"
